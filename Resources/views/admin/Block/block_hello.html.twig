{% extends sonata_block.templates.block_base %}

{% block block %}
    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <div class="box box-primary">
            <div class="box-body">
                <h3 class="profile-username text-center">Bonjour, {{ app.user.firstname }} {{ app.user.lastname }}</h3>

                <p class="text-muted text-center">Date de dernière connexion : {{ app.user.lastLogin|date('d/m/Y') }}</p>
            </div>
        </div>
    {% endif %}
{% endblock %}